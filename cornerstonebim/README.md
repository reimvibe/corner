# Cornerstone BIM

Static corporate website for Cornerstone BIM Pvt. Ltd.

Open `index.html` in a modern browser, or serve this directory with any static web server. The contact and newsletter forms are presentation-only; connect them to an approved form endpoint before launch. Update the canonical domain in `sitemap.xml` and `robots.txt` if the production URL differs.
