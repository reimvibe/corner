(() => {const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const items=document.querySelectorAll('.reveal');if(reduce){items.forEach(x=>x.classList.add('visible'));return}const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.14});items.forEach(x=>io.observe(x));document.querySelectorAll('[data-counter]').forEach(el=>{const io2=new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting)return;const end=+el.dataset.counter,start=performance.now(),run=t=>{const p=Math.min((t-start)/1000,1);el.textContent=Math.floor(end*p);if(p<1)requestAnimationFrame(run);else el.textContent=end};requestAnimationFrame(run);io2.unobserve(el)}),{threshold:.7});io2.observe(el)})})();
